services:
  ci:
    build:
      context: .
      target: pdffile-ci
    image: pdffile-ci:ci
    container_name: pdffile-ci
    volumes:
      - ./test-results:/app/test-results
      - ./dist:/app/dist
    environment:
      - PYTHONUNBUFFERED=1
    entrypoint: [sleep, infinity]
