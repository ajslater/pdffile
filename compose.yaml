services:
  ci:
    build:
      context: .
      target: pdffile-ci
    container_name: pdffile-ci
    entrypoint:
      - sleep
      - infinity
    environment:
      - PYTHONUNBUFFERED=1
    image: pdffile-ci:ci
    volumes:
      - ./test-results:/app/test-results
      - ./dist:/app/dist
